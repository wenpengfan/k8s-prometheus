apiVersion: v1
kind: "Service"
metadata:
  name: prometheus
  labels:
    name: prometheus
spec:
  ports:
  - name: prometheus
    port: 9090
    targetPort: 9090
    nodePort: 30001
  selector:
    app: prometheus
  type: NodePort
